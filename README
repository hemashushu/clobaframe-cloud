How to run the unit test:
------------------------

1. Pass the clobaframe all unit test first.

2. Check out source code to the folder:
    ~/projects/archboy/clobaframe-amazon

3. Create file:
    ~/projects/security/AwsCredentials.properties

Write the following lines into the file:

secretKey=n/a
accessKey=n/a

4. Run unit test with Apache Maven:
    $mvn clean test
